@extends('admin.layout.app')
@section('title', 'Vendors')
@section('content')
@include('admin.partials.flash')
@include('admin.vendor.breadcrumb')
<div class="card"><div class="card-header admin-card-toolbar"><h3 class="card-title">Vendors</h3><button class="btn btn-primary btn-sm" data-bs-toggle="modal" data-bs-target="#vendorCreateModal"><i class="fas fa-plus mr-1"></i>Add Vendor</button></div><div class="card-body border-bottom"><form method="GET" action="{{ route('admin.vendors.index') }}" class="form-row"><div class="col-md-6 mb-2"><input type="text" name="q" value="{{ request('q') }}" class="form-control" placeholder="Search vendor"></div><div class="col-md-2 mb-2"><button class="btn btn-info btn-sm">Apply</button></div><div class="col-md-2 mb-2"><a href="{{ route('admin.vendors.index') }}" class="btn btn-secondary btn-sm">Reset</a></div></form></div><div class="card-body table-responsive p-0"><table class="table table-hover text-nowrap"><thead><tr><th>#</th><th>Name</th><th>Services</th><th>Contact</th><th>Rate Card</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($vendors as $vendor)<tr><td>{{ $vendor->id }}</td><td>{{ $vendor->name }}</td><td>@forelse($vendor->services as $service)<span class="badge badge-info mr-1">{{ $service->name }}</span>@empty{{ $vendor->service_type ?: 'N/A' }}@endforelse</td><td>{{ $vendor->contact_person ?: 'N/A' }}<br><small>{{ $vendor->phone ?: '' }}</small></td><td>{{ $vendor->rate_card !== null ? number_format((float) $vendor->rate_card, 2) : 'N/A' }}</td><td><span class="badge {{ $vendor->status ? 'badge-success' : 'badge-secondary' }}">{{ $vendor->status ? 'Active' : 'Inactive' }}</span></td><td class="d-flex"><button class="btn btn-info btn-xs mr-1 btn-edit-vendor" data-action="{{ route('admin.vendors.update', $vendor) }}" data-name="{{ $vendor->name }}" data-service_type="{{ $vendor->service_type }}" data-service_ids="{{ $vendor->services->pluck('id')->implode(',') }}" data-contact_person="{{ $vendor->contact_person }}" data-phone="{{ $vendor->phone }}" data-email="{{ $vendor->email }}" data-address="{{ $vendor->address }}" data-rate_card="{{ $vendor->rate_card }}" data-status="{{ $vendor->status ? '1' : '0' }}" data-notes="{{ $vendor->notes }}">Edit</button><form method="POST" action="{{ route('admin.vendors.destroy', $vendor) }}">@csrf @method('DELETE')<button class="btn btn-danger btn-xs" onclick="return confirm('Delete vendor?')">Delete</button></form></td></tr>@empty<tr><td colspan="7" class="text-center">No vendor records found.</td></tr>@endforelse</tbody></table></div><div class="card-footer">{{ $vendors->links() }}</div></div>
@include('admin.vendor.model-create-update')
@stop
@section('css') @vite(['resources/css/app.css']) @stop
@section('js') @vite(['resources/js/app.js']) @include('admin.vendor.vendor-js') @stop
