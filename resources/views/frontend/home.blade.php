@extends('frontend.layouts.app')
@section('content')
<section class="py-5 bg-dark text-white"><div class="container"><h1 class="display-5">Modern Event Management System</h1><p class="lead mb-0">Fully dynamic events, roles, permissions, settings, notifications, and integrations.</p></div></section>
<section class="py-5"><div class="container"><h3 class="mb-4">Featured Events</h3><div class="row g-3">@forelse($featuredEvents as $event)<div class="col-md-4"><div class="card h-100 shadow-sm">@if($event->image)<img src="{{ asset('storage/'.$event->image) }}" class="card-img-top" alt="{{ $event->title }}">@endif<div class="card-body"><h5>{{ $event->title }}</h5><p class="text-muted">{{ $event->start_at?->format('d M Y H:i') }} | {{ $event->venue }}</p><a href="{{ route('events.show', $event) }}" class="btn btn-sm btn-primary">View Details</a></div></div></div>@empty<div class="col-12"><p>No featured event found.</p></div>@endforelse</div></div></section>
<section class="pb-5"><div class="container"><h3 class="mb-4">Upcoming Events</h3><div class="row g-3">@foreach($events as $event)<div class="col-md-4"><div class="card h-100 border-0 shadow-sm"><div class="card-body"><h5>{{ $event->title }}</h5><p class="mb-1"><i class="fa-regular fa-calendar"></i> {{ $event->start_at?->format('d M Y H:i') }}</p><p class="mb-2"><i class="fa-solid fa-location-dot"></i> {{ $event->venue }}</p><a href="{{ route('events.show', $event) }}" class="btn btn-outline-dark btn-sm">Details</a></div></div></div>@endforeach</div><div class="mt-4">{{ $events->links() }}</div></div></section>
@endsection
